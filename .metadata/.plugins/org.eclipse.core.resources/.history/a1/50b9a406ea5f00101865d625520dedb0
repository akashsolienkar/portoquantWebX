package com.quant.portoquant.infrastructure.repository;

import org.springframework.stereotype.Repository;

@Repository
public interface assetNew extends JpaRepository<HistoricalAssetMetadata, UUID> {
    Optional<HistoricalAssetMetadata> findByNameAndAssetType(String name, AssetType type);
}
